/target/

# Local Computed Redb Files with merkle trees can be regenerated using integration/test/build_fixture.rs
/integration/tests/fixtures

# Local test secrets: mnemonic + expected vectors. See .env.example for the keys.
/.env

# Work-in-progress crates, parked until ready to publish (see CLAUDE.md).
/crates/railgun-prover/
/crates/railgun-wallet-native/

# Forward-looking design docs, kept local until reconciled with the current code.
/ARCHITECTURE.md
/RAILGUN_WALLET_RUNTIME.md
