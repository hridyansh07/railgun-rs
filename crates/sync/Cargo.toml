[package]
name = "sync"
description = "Chain event sync (Subsquid GraphQL) into the RAILGUN commitment store."
edition.workspace = true
license.workspace = true
publish.workspace = true
version.workspace = true

[dependencies]
alloy-primitives = { workspace = true }
async-trait = { workspace = true }
# The per-window commit folds new leaves through the merkle accumulator so the
# frontier snapshot lands atomically with the data it summarizes.
crypto = { workspace = true }
database = { workspace = true }
futures-timer = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
types = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true }
types = { workspace = true }

[lints]
workspace = true
